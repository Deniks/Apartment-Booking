{
  "name": "server",
  "version": "1.0.0",
  "description": "deploy config",
  "scripts": {
    "production": "concurrently \"node server/dist/index.js\" \"cd client && npm run start\""
  },
  "engines": {
    "node": "12.x"
  }
}
